# Streamlit UI
streamlit==1.49.1

# PyTorch
torch==2.8.0   # CPU version, GPU optional depending on cloud

# HuggingFace Transformers
transformers  

# Image handling
Pillow>=10.0.1  

# Word documents
python-docx>=0.8.12  # exact 0.8.12 unavailable, >= works

# Logging & Rich display
rich   # latest compatible version
markdown-it-py   # required by rich
pygments        # required by rich
mdurl           # required by markdown-it-py
